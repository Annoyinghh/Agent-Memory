mcp>=1.1.2
chromadb>=0.5.0
pydantic>=2.0.0
litellm
fastapi
uvicorn
networkx  # Louvain community detection (get_architecture); also a graphify transitive dep
# headroom-ai[all] — optional reversible context compression (code/JSON/logs/prose).
# Installed via the Dockerfile in a BEST-EFFORT retry loop (large: pulls PyTorch + Kompress).
# If the build network cannot fetch it, the image still builds and compression silently
# degrades to passthrough at runtime. Check live status via the headroom_stats MCP tool or
# GET /api/compress/stats.
